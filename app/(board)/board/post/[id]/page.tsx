import classNames from 'classnames/bind';

import style from './PostID.module.scss';

const cx = classNames.bind(style);

const Page = () => {
  return <main className={cx('container')}>sss</main>;
};

export default Page;
