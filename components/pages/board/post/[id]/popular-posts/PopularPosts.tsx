import classNames from 'classnames/bind';

import style from './PostDetail.module.scss';

const cx = classNames.bind(style);

const PopularPosts = () => {
  return <section className={cx('container')}>222</section>;
};

export default PopularPosts;
