import classNames from 'classnames/bind';

import style from './PostDetail.module.scss';

const cx = classNames.bind(style);

const PostDetail = () => {
  return <article className={cx('container')}>11</article>;
};

export default PostDetail;
