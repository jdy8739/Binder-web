'use client';

import classNames from 'classnames/bind';

import style from './PostAnswering.module.scss';

const cx = classNames.bind(style);

const PostAnswering = () => {
  return 'post answering';
};

export default PostAnswering;
