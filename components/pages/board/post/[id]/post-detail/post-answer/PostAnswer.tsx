'use client';

import classNames from 'classnames/bind';

import style from './PostAnswer.module.scss';

const cx = classNames.bind(style);

const PostAnswer = () => {
  return <div className={cx('card', 'answer')}>1</div>;
};

export default PostAnswer;
